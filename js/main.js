/**
 * BVR Promos - Hero Section Script
 * Lightweight script for background video handling & interactions
 */

// document.addEventListener('DOMContentLoaded', () => {
//     const heroVideo = document.querySelector('.hero-video');

//     // Ensure background video plays smoothly across all browsers
//     if (heroVideo) {
//         const playPromise = heroVideo.play();
//         if (playPromise !== undefined) {
//             playPromise.catch(() => {
//                 // If autoplay is blocked by browser policy, ensure it is muted and retry
//                 heroVideo.muted = true;
//                 heroVideo.play();
//             });
//         }
//     }
// });
